{% extends 'base.html.twig' %}

{% block title %}Taxe index{% endblock %}

{% block body %}
    <h1>Taxe index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Contry_code</th>
                <th>Rate</th>
                <th>Type</th>
                <th>IsActive</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for taxe in taxes %}
            <tr>
                <td>{{ taxe.id }}</td>
                <td>{{ taxe.name }}</td>
                <td>{{ taxe.contryCode }}</td>
                <td>{{ taxe.rate }}</td>
                <td>{{ taxe.type }}</td>
                <td>{{ taxe.isActive ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_taxe_show', {'id': taxe.id}) }}">show</a>
                    <a href="{{ path('app_taxe_edit', {'id': taxe.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_taxe_new') }}">Create new</a>
{% endblock %}
