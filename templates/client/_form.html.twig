{{ form_start(form) }}
    {{ form_row(form.raisonSocial) }}
      {{ form_row(form.nameStreet) }}
      {{ form_row(form.nameStreet2) }}
      <div class="d-flex gap-3 col-md-6 justify-between">
        {{ form_row(form.codePostal) }}
        {{ form_row(form.ville) }}
        </div>
        <div class="d-flex gap-3 col-md-6 justify-between">
        {{ form_row(form.businessPhone) }}
        {{ form_row(form.mobilePhone) }}
</div>
        {{ form_row(form.email) }}

                        <button class="btn-ocean btn-ocean-primary mt-3">{{ button_label|default('Créer') }}</button>
                    {{ form_end(form) }}